## Teoria de Funcional JS 

### Explicado desde lo aprendido con mis palabras

**Funcionales**

1. El funcional hace referencua a poder elevar el envio de parametros de una funcion con otra funcion como se ve en la practica 1 almacenas la funcion y mandas a llamar una funcion usando otra funcion para almacenar en otra variable esto con la ntencion de nunca modificar la variable original es lo recomendable 

2. Al almacenar una funcion en otra se puede usar la funcion deseada al usar una funcion que llame a la funcion que quieras utilizar en este caso usamos la funcion fn para llamar a la funcion suma o multiplicacion

3. Realizamos un filtrado mediante una funcion en el cual ponemos un metodo de filtrado a los key-name de el objeto carrito

4. en esta practica se realiza un mapeo a solo los valores (nombres de los articulos) 

5. convinacion de las dos precticas anteriores un filtro con mapeo

**Funcionales puras**
6. Funciones Puras basicamente es almacenar en nuevas variables los resultados de las variables y dejar las originales intactas

7. funcion que lleva otra funcion usadas mayor mente en librerias

8. funcion que lleva otra funcion esta se almacena en otra variable y se manda a llamar a la funcion dentro de la funcion

**Funciones Partial & curring**
9. Funciones Parciales  en lugar de pedir 3 datos haces funcion anidada por dato es importante conocer mas que usar

10. En la practica 10 podemos ver la otra manera de usar algo como la herencia en POO pero esto se llama composicion son JS funciona igual hasta donde se puede ver pero la sintaxis es distinta ya que separas cada uno de las funciones y metodos y las usas dependiendo el objeto que las necesite 

